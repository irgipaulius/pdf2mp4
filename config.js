export default {
  /** As a rule of thumb, enter a number of CPU threads 
   * you wish to utilize during rasterization.
   * Entering a higher number will be faster,
   * but the system will lock up with CPU pinned at 100% */
  maxConcurrency: 8,
};
